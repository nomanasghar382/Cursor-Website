Place production GLB product assets in this directory.

Recommended files:

- `nexora-device.glb`
- `luxury-bottle.glb`
- `headphones.glb`
- `smart-watch.glb`

The current scene uses procedural PBR product geometry so the experience runs without external assets. These filenames can replace the procedural components when final Blender or Spline models are ready.
