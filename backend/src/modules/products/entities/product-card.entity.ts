export interface ProductCard {
  id: string;
  name: string;
  category: string;
  price: number;
  rating: number;
  stock: number;
  badge: string;
  aiMatch: number;
  description: string;
  gradient: string;
}
